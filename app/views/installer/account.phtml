
<nav>
	<img src="/app/views/assets/img/logo.png">
	<ul>
		<li>Language and timezone</li>
		<li>Database setup</li>
		<li>Site metadata</li>
		<li class="elapsed">Your account</li>
		<li>All done!</li>
	</ul>
</nav>

<div class="content">
	<h1>Your first account</h1>
	<p>Oh, we're so tantalisingly close! All we need now is a username and password to log in to the admin area with.</p>

	<?php echo $form->open(); ?>

	<?php if(count($messages)): ?>
	<ul class="errors">
		<?php foreach($messages as $message): ?>
		<li><?php echo $message; ?></li>
		<?php endforeach; ?>
	</ul>
	<?php endif; ?>

	<?php foreach($form->getElementsExcluding(['submit']) as $element): ?>
	<p><label for="<?php echo $element->getName(); ?>"><?php echo $element->getLabel(); ?></label> <?php echo $element->getHtml(); ?></p>
	<?php endforeach; ?>

	<p><a class="button" href="<?php echo $this->url('index.php?installer=metadata'); ?>">Back</a>
		<?php echo $form->getElement('submit')->getHtml(); ?></p>

	<?php echo $form->close(); ?>
</div>

<small>
	You’re installing Anchor version 1.0.
	<a href="//twitter.com/anchorcms" target="_blank">Need help?</a>
</small>
