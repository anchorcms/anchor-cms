<?php if(count($messages)): foreach($messages as $type => $group): ?>
	<?php foreach($group as $message): ?>
		<div class="msg <?php echo $type; ?>">
			<?php echo $message; ?>
		</div>
	<?php endforeach; ?>
<?php endforeach; endif; ?>