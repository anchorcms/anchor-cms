
<div class="wrap">
	<header class="heading">
		<h2>Categories</h2>

		<div class="button-group">
			<p><a class="button button--primary" href="/admin/categories/create">Create a new category</a></p>
		</div>
	</header>

	<?php echo $messages; ?>

	<main>
		<ul class="listing">
			<?php foreach($categories as $category): ?>
			<li>
				<h3><a href="/admin/categories/<?php echo $category->id; ?>/edit"><?php echo $category->title; ?></a></h3>
				<span class="badge badge--pinned"><?php echo $category->slug; ?></span>
			</li>
			<?php endforeach; ?>
		</ul>

		<div class="pagination">
			<?php echo $paging->links(); ?>
		</div>
	</main>
</div>
