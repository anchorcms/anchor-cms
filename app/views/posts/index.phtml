<section class="filter wrap narrow">
	<form method="get" action="/admin/posts">
		<input name="search" type="search" placeholder="Search posts…">

		<select class="autoupdate" data-append="status" name="status">
			<option data-reset value="">All</option>
			<?php foreach(['published', 'draft', 'archived'] as $status): ?>
			<option value="<?php echo $status; ?>"><?php echo ucfirst($status); ?></option>
			<?php endforeach; ?>
		</select>
	</form>

	<a class="btn primary" href="/admin/posts/create">Create a new page</a>
</section>

<section class="panel wrap narrow drilldown">
	<?php echo $messages; ?>

	<?php if(!empty($posts)): ?>
		<ol>
			<?php foreach($posts as $post): ?>
			<li class="has-link">
				<a href="/admin/posts/<?php echo $post->id; ?>/edit">
					<span class="post-name">
						<b class="realname"><?php echo $post->title; ?></b>
						<span class="slug">/posts/<?php echo $post->slug; ?></span>
					</span>

					<ul>
						<li><span class="badge status"><?php echo $post->status; ?></span></li>
						<li><time><?php echo date('jS M Y', strtotime($post->created)); ?></time></li>
					</ul>
				</a>
			</li>
			<?php endforeach; ?>
		</ol>

		<nav class="paging"><?php echo $paging->links(); ?></nav>
	<?php else: ?>
		<span class="empty-state flaticon feather-1">Click a post on the left to edit, or add a post at the top-right.</span>
	<?php endif; ?>
</section>
