
<?php echo $form->open(); ?>

	<?php echo $form->getElement('_token')->getHtml(); ?>

	<?php echo $this->render('partials/content', ['form' => $form]); ?>

	<div class="wrap">
		<p><?php echo $form->getElement('submit')->withAttributes(['class' => 'button button--primary'])->getHtml(); ?>
			<a class="button button--danger" data-confirm="Are you sure?" href="/admin/posts/<?php echo $post->id; ?>/delete">Permanatly Delete</a></p>
	</div>

<?php echo $form->close(); ?>

<?php echo $this->render('partials/messages-float', ['messages' => $messages]); ?>

<script>
	(function() {
		var status = document.querySelector('select[name=status]'),
			date = document.querySelector('input[name=published]');

		var toggle = function() {
			var val = status.options[status.selectedIndex].value,
				tog = val == 'published' ? 'remove' : 'add';
			date.parentNode.parentNode.classList[tog]('display--hidden');
		};

		status.addEventListener('change', toggle, false);
		toggle();
	})();
</script>
